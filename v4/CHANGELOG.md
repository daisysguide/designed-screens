# v4 changelog

v4 implements the design decisions surfaced during the v3 review. v3 remains
in the repo as the reviewed baseline (no further changes will be made to it).

## Summary of v3 → v4 changes

- **Topic state model:** five states for regular topics (Not started · Your turn · Waiting · Pending · Resolved) plus Locked for prediction cards. Replaces the previous four-state model.
- **Screen 21 redesign:** two reflection options (Resolved · Needs more work), selection required, single "Submit →" CTA. "Talk about it" and "Revisit later" merged into "Needs more work."
- **Snapshot UI cut:** "Original answers" toggle removed from Screen 23. Data retained server-side but not surfaced.
- **Dual view toggle (Category / Stage):** added to Screen 14 (Topic List) and Screen 12 (Progress).
- **Progress screen overhaul:** stacked per-group bars; chaos meter hidden until 3 reveals; alignment % hidden until 5 reveals.
- **Settings additions:** Q1 journey-stage update row in Pacing section.
- **Edit-while-waiting:** "Change my answers" affordance on Screens 19 and 16 Waiting state.
- **Re-answer flow mechanics:** paired round with 14-day expiry; V1.1 will add a unilateral-vs-paired choice.
- **Prediction Cards eligibility:** only shown to users with a future milestone date.
- **No time-gating in V1** except Prediction Cards.
- **Paywall feature list unified** between Screens 09 and 27.
- **Unpaid returning user routing:** Personalized Results → Manage Topics → Paywall.
- **Topic time estimate:** Empty Home shows range ("5–10 minutes"); Topic Intro stats become dynamic per-topic.
- **Visual consistency:** Topics icon 📚 everywhere; partner status "Linked ✓"; American spelling; sample user "Alex"; area code 972.
- **Slider initial state:** ghosted thumb at midpoint instead of fully hidden.

## What's deferred to v1.1 (intentionally out of scope in v4)

- Re-answer flow choice: "update just your answers" vs "start the discussion over"
- Full Q1 update UX with "Rebuilding your plan…" animation and diff screen

## Dated changes

### 2026-06-02

- **Screen 13 (Settings) — Account / Partner / Notifications restructure + Founding member detail.** Scoped entirely to `13-settings.html`; `styles.css` not touched. **Account section** reordered to Name → Phone number (read-only) → Lifetime access → Delete account → Log out: Restore purchase row removed from Account; Phone number row stripped of chevron and made read-only (spec note: label varies by auth path — "Phone number" / "Email" / "Connected with Apple" / "Connected with Google" — read-only in all cases); new **Lifetime access** row (value "Founding member" + chevron) navigates to a new push sub-screen. **Founding member sub-screen** (Edit-Name push pattern): proud card (light-purple `rgba(95,69,242,0.08)` bg, solid-purple "Founding member" pill in white, Grandstander "You're in for good.", sub "One purchase, lifetime access for both you and Jordan."), "What that covers" white card with three purple-check rows, editorial thank-you note on cream (no card, muted "The Daisy's Guide team" signature), "Manage" white card with **Restore purchase relocated here** (same spinner + green/dark toast behavior), Terms of Service, and "You & your data" (privacy URL, warmer label). Account Deletion sheet bullet 3 left untouched ("use 'Restore purchase' in Settings to recover access" still holds — Restore lives one level deeper but "in Settings" is accurate). **Partner row** drops the avatar circle (avatars being removed app-wide; partner identity will ride on name + a per-partner accent color, confirmed at Alignment Reveal); row is now name + Linked ✓ + chevron and pushes to a new **Partner detail sub-screen** (Linked-partner card + "Unlink partner" action row in `status-error-text`). **Unlink confirmation bottom sheet** mirrors the Account Deletion sheet structure: title "Unlink from {name}?", three bullets (answers/reveals stay · connection removed and slot shows Disconnected · can't reveal new topics until relink), destructive "Unlink" (`pink-400` fill, `status-error-text` label) + ghost "Cancel". Spec captures the pairing model: shared reveal history is keyed to the **unordered pair of account IDs**, never to the connection — unlink **deactivates** the pairing (never deletes), re-linking the same partner reactivates it and restores all prior reveals, linking a new partner creates a fresh pairing while the user's own per-topic answers persist. **Notifications section rebuilt** top-to-bottom: (1) conditional **OS-permission-denied banner** above everything else — soft warning tone (orange band `rgba(255,173,108,0.34)` / text `#995611`, bell-slash icon), **not** error red — copy "Notifications are turned off in your phone settings." + "Open Settings" button (deep-links to OS settings on iOS/Android, re-checks on foreground); (2) **Partner activity** toggle (on by default, transactional events — partner linked/accepted, your turn, ready to reveal — fires on the event and **ignores Nudge frequency entirely**), with an inline off-warning attached **inside the card** in the orange band: "With this off, we won't let you know when Jordan answers or a reveal is ready. You'll have to remember to check back yourself."; (3) **Nudge frequency** picker **rescoped to engagement reminders only** (stalled-topic nudges) with new subtext "How often we remind you about waiting conversations." — stale "most important setting for retention / first in the list for a reason" note dropped, since Partner activity is now the retention-critical control; (4) **Funny push notifications** toggle reframed as a **tone layer** (changes voice of pushes that fire anyway, does not create standalone pushes). Spec panel rewritten: new cards for Notifications structure, OS-denied banner, Partner activity + off-warning, Event inventory (V1 transactional/engagement/tone matrix · V1.1 milestone & stage pushes deferred), Phone number read-only, Partner row + detail, Unlink sheet, Pairing model, Lifetime access + Founding member screen; existing Restore Purchase card rewritten to note its new home in Founding member; nav-flow card updated to surface the new push targets; "All settings — full reference" block and "Components on this screen" tags updated; key-notes refreshed. Page-desc rewritten. Two new demo controls above the phone: an existing Q1 tab strip and a new "Push permission" tab strip (Granted / Denied) that toggles the OS-denied banner. All new sub-screens and the unlink sheet are interactive in the preview via inline `onclick` wiring; no `styles.css` changes; `body.page-23` (page-NN drift) left as-is per the repo-hygiene note. No em-dashes in any on-screen mockup copy (the pre-existing Account Deletion bullet 2 em-dash is left untouched per the prompt's "leave bullet 3 as-is" framing, and spec/doc copy retains em-dashes per house style). Note for follow-up: Terms of Service and a privacy link now exist in both the Legal section card and the new Founding member "Manage" list — the prompt explicitly relocated only Restore purchase but listed Terms + "You & your data" in Manage without saying to remove them from Legal; left Legal intact to preserve the established placement, flag in case a follow-up wants Legal slimmed down to avoid duplication.

- **Screen 16 (Topic Intro) — partner pill moves under the blurb.** One small placement change on top of the previous fix-ups. The avatar-free Partner Status Block is now the closing element of the content group (eyebrow → title → blurb → pill) rather than sitting immediately above the CTA stack. CSS: `margin-bottom: 20px` on `.partner-pill` swapped for `margin-top: 12px` so the pill attaches to the blurb with a small gap. DOM: pill moved above `.spacer` and the spacer now sits between the content group and the CTA block — leftover vertical slack therefore reads as **breathing room above the buttons**, not as a gap around the pill. The pill's size, centering, six states, color language, and animations are unchanged; the CTAs remain anchored at the bottom via the same `.spacer { flex: 1 }`. Spec panel updated: Partner Status Block "Position" row rewritten to describe the under-the-blurb placement and the 12px attach gap; intro paragraph reworded ("cap the content group" vs. "sit above the CTA stack"); behavior note and layout-fits-without-scrolling note updated to reflect the new top-to-bottom rhythm. `body.page-13` left as-is per the page-NN drift hygiene note.

- **Screen 16 (Topic Intro) — layout fix-ups.** Three small refinements to the screen as built after the previous redesign: (1) **Taller illustration:** `.illus-slot` bumped 160 → **230px** (override in the scoped `<style>` block in the screen file — `styles.css` not touched). Card chrome unchanged (white card, dashed corner marks, no shadow, label). Placeholder copy updated to match ("160px" → "230px"); spec card's Height row updated with a "was 160px" annotation. (2) **Partner pill stops going full-width:** the avatar-free pill was rendering as a full-width band reading like a third stacked button. Now `display: inline-flex; align-self: center;` so the pill **hugs its contents and centers in the column** across every state (cyan / gray / neutral turn-states and purple pre-join prompts). `.pill-copy` dropped from `flex: 1` to `flex: 0 1 auto` so the copy doesn't stretch the chip back to full width; `max-width: 100%` retained as an overflow safety. Each state's internal treatment (leading dot or icon, copy, tint, chevron on pre-join) is unchanged. (3) **Vertical rhythm:** bottom margin under the pill bumped 14 → **20px** so it sits visually apart from the CTA stack; with the taller hero and the now-narrow chip, the existing `.spacer { flex: 1 }` absorbs the remaining slack above the pill rather than as one large mid-screen gap. Verified visually in three states (Not started / No partner yet / Ready to reveal). Spec panel updated throughout: Partner Status Block "Layout" row gains the inline-flex / status-chip framing, "Position" row notes centered + 20px-margin-below + applies-to-every-state; the block's intro paragraph reframes the pill as "centered, content-hugging status chip — not a full-width band that would read as a third button"; behavior note for the Partner Status Block rewritten to mention "centered, content-hugging" and the "third CTA" pitfall; layout-fits-without-scrolling note expanded to the composed top-to-bottom rhythm explicitly. `body.page-13` left as-is per the page-NN drift hygiene note.

- **Dashed-border convention reconciled to "supplemental, not AI."** Wording-only sweep across the design-system doc and four v4 spec panels; no card or ✨ is restyled, no layout changes. The dashed border now consistently means "supplemental or authored content distinct from the main flow" (shared by For You Two, Credibility, Resources, Prediction Card Header) — the design-system doc already said this; several spec panels still contradicted it. Added a one-line principle in the doc: **no styling in the app is used to denote AI authorship**. The ✨ + "for you two" label are reframed as the **personalized-synthesis mark** (a reflection about the couple), not an AI flag — the distinction between a generated reflection and expert content is carried by framing ("for you two" vs. "medically reviewed"), not by styling. Captured a **medical-authority guardrail** in the doc and the For You Two Card rules: a personalized synthesis must not visually borrow the "medically reviewed" authority that expert/authored content carries. Edits: `design system.md` (For You Two card description + rules, cross-cutting "Dashed border treatment" section, 2026-05-27 release note); `20-alignment-reveal.html` L426 (For You Two border row); `12-progress.html` L299 + L322 (easter-egg hard rules + Dashed border token row); `14-topic-list.html` L717 ("Needs you" no-sparkle rationale reworded); `09-paywall.html` L239 (Demo Reveal Card row relabeled "For you two synthesis"). Logged the reconciliation in `v3/dg-v3-decisions-log.md` §16 (the decisions log continues to apply through v4 per CLAUDE.md). Frozen v1/v2/v3 occurrences left untouched. Key principles table row 31 was already correct ("not AI-specific") and untouched.

- **Screen 17 (Topic Article) — redesign.** Background bug fixed: `body.page-14 .screen-root .phone` was `--white`, now `--cream` to match every other screen. Chrome reworked: old `.article-nav` row (`‹ Back to topic` + `3 min read`) replaced with the flow chrome top bar — bare `‹` top-left (back one step, returns to whatever pushed this screen; Topic Intro when entered from 16, the Question when opened mid-question from 18), `✕` top-right (exit the whole flow to Topic List), pinned and always visible while the article scrolls. Swipe-down-from-top mirrors `✕`, gated to scroll-top + non-interactive gesture origin (matters here since this screen scrolls); no confirm dialog. The "back always returns to Screen 16" rule is dropped. Read-time moved out of the chrome and into the byline as metadata ("Medically reviewed · 3 min read"). Byline avatar bumped 36 → 42px and the `🌼` emoji placeholder replaced with the real Dr. Daisy headshot (`images/dr-daisy.jpg` referenced from `../images/dr-daisy.jpg`), rendered with `object-fit: cover` — the author byline keeps a real photo (avatar removal is partner-only). New **epigraph** element added below the byline: italic DM Sans 16px @ line-height 1.5, `text-primary` @ ~0.8, with the attribution on its own line (13px `text-tertiary`, not italic) — templated per topic. Dummy last-name article replaced with the real Village article ("Who will we trust to watch the baby or babysit?") laid into the existing section-heading + body pattern plus new bulleted-list style. Resources card omitted (this topic has none; per-topic optional, returns with the dashed treatment when present). Emoji feedback block and its dashed separator removed — per-article feedback is intentionally deferred as a future test; the guide now ends on its content. Sticky CTA stays always-visible (no hide-on-scroll, no back-to-top) and is now **context-aware**: "Start answering →" when entered from the intro, "Return to questions →" when opened mid-question. Spec panel updated throughout (nav-flow, chrome card, article structure with epigraph, resources as per-topic optional, sticky-CTA labels, deferred-feedback note, cream background). Em-dash audit: no em dashes anywhere in on-screen copy (epigraph attribution uses an en-dash); spec/doc copy retains them. `body.page-14` left as-is per the page-NN drift hygiene note. All new component CSS lives in a scoped `<style>` block inside the screen file; `styles.css` is not touched.

- **Screen 16 (Topic Intro) — redesign.** Stats card (Questions / Min read / Min quiz) and its spec card removed; read length moves to Screen 17, question count becomes Screen 18's "1 of N" progress. Chrome reworked to the topic-flow modal model: top-left empty (this is the flow's entry screen), ✕ top-right exits to Topic List, swipe-down-from-top mirrors the ✕ (gated to scroll-top + non-interactive start), no confirm dialog. Ghost "Back to topics" removed from every state. Partner status line replaced by an **avatar-free Partner Status Block** (pill variant): 13px DM Sans 600, state-tinted, sitting above the CTA stack. State switcher expanded from four to six: "No partner yet" + "Invite pending" added as tappable purple pre-join variants (leading invite/clock icon + trailing ›, route to Screen 10); joined turn-states reuse Screen 14's pill color language with a leading status dot. Ready-to-reveal carries Screen 14's reveal-pill "charge" treatment verbatim (breathing purple glow + always-on baseline halo; static baseline halo only under `prefers-reduced-motion`). Per-state CTA stack reconciled (see spec table). "Two equal CTAs" / "two equal paths" / "visually equal weight" language removed from spec and info-box; reframed as "answering leads, guide is supporting and also reachable mid-question." New "Flow navigation model" spec card documents the modal/horizontal-push/✕ convention once at the flow's entry point (applies through Screen 23, with per-screen exceptions called out where relevant). Em-dash audit: phone-mockup copy is dash-free; spec/doc copy retains them. `body.page-13` left as-is per the page-NN drift hygiene note. All new component CSS lives in a scoped `<style>` block inside the screen file; `styles.css` is not touched.

### 2026-05-31

- **Screen 12 (Progress) — full redesign ported in.** Phone mockup rebuilt from `12-progress-redesign.html`; spec panel rewritten against `screen-12-progress-spec.md`. Out: 3-column "74% Aligned" stats slab, "We're good / Needs work / In progress" stacked per-category bar, chaos meter, header-mounted grouping toggle. In: completion lede + 10px bar; alignment distribution card (state-aware note, 14px bar, hybrid "Worth a conversation" row with revisit list); inline grouping toggle as the section header for breakdown (52px tap-target rows, 8px bar, navigate to that group's accordion on Topics); badges with dashed teaser; in-between and empty states. Progress ends after badges — easter egg moves to Home (Screen 11). Doc-page state switcher added above the phone for reviewers (Populated · Balanced · Mostly settled · Orange-heavy · All orange · All aligned · In-between · Nothing answered). Page-desc reworded. The scoping bug (Screen 12 living under `body.page-22`) is left in place per CLAUDE.md repo-hygiene note — the new Progress styles overwrite the existing `body.page-22 .screen-root` block; the eventual `page-22 → page-12` rename will happen in the dedicated page-NN sweep that also fixes Screen 14 (`page-12 → page-14`).

### 2026-05-28

- **Screen 08 (Personalized Results) — Conversation Preview labels unified:** all section labels now share a single style (small uppercase `purple-500`), identical across the personalized and fallback states. The "from your answers" vs "curated" distinction is carried by dot color + wording, never by label color.
- **Screen 08 — scope line reworded:** "Plus N more, across all 7 areas of parenting prep." → "Plus N more, with expert guidance for everything ahead."
